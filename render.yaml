services:
  - type: web
    name: teamap
    runtime: python
    plan: free
    buildCommand: echo "No build step"
    startCommand: python3 server.py
    healthCheckPath: /
    envVars:
      - key: GOOGLE_MAPS_API_KEY
        sync: false
